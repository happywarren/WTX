<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lt.user.core.dao.sqldb.IChannelDao">
    <select id="getChannelByCode" parameterType="String" resultType="com.lt.model.user.Channel">
		select * from channel where code = #{code}
	</select>
</mapper>